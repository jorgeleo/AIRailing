# Hawthorne Analyzers

Hawthorne is a Roslyn analyzer package for finding unnecessary abstraction,
indirection, hidden global state, and structural complexity in C# projects.

The package is under active development. Its rules are warnings by default and
will be configured through `hawthorne.json`.
