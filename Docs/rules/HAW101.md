# HAW101

Reports cyclomatic complexity above `maximum` (default 10).

HAW101 is non-suppressible. Simplify the control flow by reducing independent
paths; HAW901 reports a source suppression as an error.
