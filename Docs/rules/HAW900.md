# HAW900

Reports invalid Hawthorne configuration as a compiler error; normal analysis is
skipped. HAW900 is non-suppressible: correct the configuration instead of
adding a source suppression. HAW901 reports an attempted suppression as an
error.
