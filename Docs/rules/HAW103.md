# HAW103

Reports semantic control-flow nesting above `maximum` (default 4). Expression-bodied methods are measured through their expression.

HAW103 is non-suppressible. Use guard clauses or extract focused work to
simplify nesting; HAW901 reports a source suppression as an error.
