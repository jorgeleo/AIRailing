# HAW004

Reports a stored static self instance with mutable instance state. Factory members that create a fresh value are not shared state, and `init`-only properties are treated as immutable. Configure `requireMutableState` to include immutable singleton-style values.
