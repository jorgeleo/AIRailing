# HAW001

Reports a source-defined interface with exactly one concrete implementation in the current compilation. Referenced framework and library contracts are excluded.

Review whether the interface has a demonstrated runtime or test boundary; otherwise, add another implementation or remove the abstraction. A separate test assembly is not part of the production compilation, so intentional seams should use `SuppressMessageAttribute` with a non-blank `Justification`.
