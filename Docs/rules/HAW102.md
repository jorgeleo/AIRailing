# HAW102

Reports nesting-weighted cognitive complexity above `maximum` (default 15). Expression-bodied methods are measured through their expression.

HAW102 is non-suppressible. Simplify the mental branching and nesting;
HAW901 reports a source suppression as an error.
