# HAW105

Reports methods above executable-statement or physical-line limits (defaults 30 and 50).

HAW105 is non-suppressible. Split the method into focused operations;
HAW901 reports a source suppression as an error.
