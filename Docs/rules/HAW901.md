# HAW901

Reports `#pragma warning disable` directives that name HAW rules, codeless disables that suppress every warning, and Hawthorne `SuppressMessageAttribute` uses with a blank justification. Use `SuppressMessageAttribute` with a non-blank `Justification` for an intentional suppression.
