# HAW003

Reports methods or types that directly forward unchanged arguments to a field, property, or parameter dependency. Transformations, fluent operations, local helper calls, overrides, and interface implementations are not reported.
